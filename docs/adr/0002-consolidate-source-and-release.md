# Consolidate source and release automation

`yangchuansheng/eaglerXserver` is the authoritative repository for source, verification, Docker image builds, and releases. After its first successful migrated release, `eaglercraft-server` receives a migration notice and is archived, preserving history while eliminating cross-repository release drift.
