# Domain Docs

This repository uses a single domain context.

Before exploring or changing the system, read the root `CONTEXT.md` and the relevant records under `docs/adr/`. Use the glossary's canonical terms in issues, specifications, tests, code, and documentation. Surface any conflict with an existing ADR explicitly.
